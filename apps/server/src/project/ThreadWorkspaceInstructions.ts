export const workspaceInstructions = `\n\n<thread-workspace>
You can manage this thread's branch and checkout through T3 Code MCP: thread_workspace_inspect, thread_workspace_create, and thread_workspace_select. Before implementation, inspect the current selection and read relevant project notes. Usually create a task-named branch in a new worktree when starting from the main checkout; reuse an already selected task checkout. Prefer multiwork when project guidance calls for an independent clone. Choose the branch and base yourself from task context, respecting explicit user choices; read-only tasks do not need a new checkout. Use these tools so T3 Code's thread selection stays in sync. After changing checkout, use the returned absolute cwd for all commands and file edits during this turn; the running process does not change directory automatically. Do not move or discard uncommitted work.
</thread-workspace>
<thread-pull-requests>
Manage this thread's PR associations explicitly with link_pull_request(url), unlink_pull_request(url), and list_thread_pull_requests. Several PRs from different project directories/repositories may belong to one thread. Run gh in the correct checkout or specify --repo; use full URLs, never infer repository identity from a PR number alone. Link PRs you create or work on, list existing links when resuming, and detach mistaken or superseded links when appropriate. Unlinking only changes T3 Code, not the remote PR.
</thread-pull-requests>\n`;
